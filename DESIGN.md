# 投票システム 設計メモ

## 概要
- 1つの質問に対する単一選択式の投票
- 同時に進行する投票テーマは1つのみ
- 結果は管理者のみ閲覧可能

## 管理者について
- 「管理者」は投票テーマの作成者(createdBy)。グローバル管理者は存在しない
- 結果(集計)は作成者のみ閲覧可能。投票者はリアルタイムでは結果を見られない
  （投票中に結果が見えるとバンドワゴン効果で後続票に影響するため）

## Firestoreデータ構造

### polls コレクション
polls/{pollId}
  - question: string        例: "好きな○○は？"
  - options: array          [{ id, text, description?, imageUrl? }, ...]
                             ※description/imageUrlは任意。voteCountは含まない（下記results参照）
  - optionIds: array        [id, ...]             ※options[].idの一覧。セキュリティルールでの検証用
  - isActive: boolean
  - createdAt: timestamp
  - createdBy: string       作成者(=管理者)のuid

### votes サブコレクション（二重投票防止・集計の元データ）
polls/{pollId}/votes/{voterUid}   ← ドキュメントIDを投票者UIDにする
  - optionId: string
  - votedAt: timestamp
  - 読み取りは作成者、または本人（自分が投票済みか確認するため）のみ。作成後は更新・削除不可

## ポイント
- 匿名認証(Anonymous Authentication)のUIDをvotesのドキュメントIDに使い、二重投票を防止
- 得票数は別カウンタを持たず、作成者が`votes`サブコレクション全体を読んでクライアント側で集計する
  （作成者はvotes全件の読み取り権限を持つが、投票者は自分の1件しか読めない）
  - 当初は`results/{optionId}.count`という書き込み可能なカウンタを別途持たせていたが、セキュリティ
    監査で「votes/{uid}を作らずにcountだけ直接インクリメントし続けられる（水増しし放題）」という
    重大な不備が見つかったため撤廃した。Firestoreのルールは同一リクエスト内の別ドキュメントへの
    書き込みを検証できないため、「投票の記録」と「カウンタの増加」を別ドキュメントに分けて
    ルールだけで両者を確実にペアにすることはできない。votesドキュメント自体（1UIDにつき1件・
    作成後不変）から集計する方式なら、書き込み可能なカウンタという攻撃対象が存在しなくなる

## URL構造（クライアント側ルーティング）
- `/`                  … 状態に応じて自動的にリダイレクト（自分の投票があれば管理画面、他人の投票が
                          進行中なら投票画面、何もなければ作成フォーム）
- `/new`               … 新しい投票の作成フォーム（常にこのフォームを表示）
- `/poll/{pollId}`     … 特定の投票の投票画面（共有可能なリンク）
- `/poll/{pollId}/admin` … 特定の投票の管理画面。ただし実際に結果を見たり締め切ったりできるのは
                          Firestoreのセキュリティルール上、作成者(createdBy)のuidと一致する場合のみ。
                          作成者以外がこのURLを開いた場合は投票画面にフォールバックする
  - 現状、管理者かどうかの判定は匿名認証のuid（＝ブラウザ/端末に紐づくセッション）に依存するため、
    「管理用URLを別の端末で開けば管理者になれる」わけではない。真に別端末でも通用する共有可能な
    管理者リンクが必要になった場合は、Cloud Functionsによるカスタムトークン発行などが別途必要
- `/history`           … 自分が作成した投票の一覧（過去のものも含む）
